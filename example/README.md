# hardware_barcode_scanner example

A minimal app that starts `HardwareScannerWidget` and displays the latest HID or
Android broadcast scan with its normalized format and source.

Connect a scanner or use a rugged device configured with one of the package's
default broadcast presets, then run:

```sh
flutter run
```
